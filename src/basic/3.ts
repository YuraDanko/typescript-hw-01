// Змінна, яка може містити або рядок, або число (union type)
let value: string | number;

// Змінна, яка може містити лише значення 'enable' або 'disable' (literal type)
let status: "enable" | "disable";
